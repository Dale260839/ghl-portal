# EOD — BuildSuite™ Three-Experience Platform

**Period:** 2026-07-30 → 2026-07-31 (2 days) · **Total: 8.0 hrs**
**Project window:** 2026-07-31 → 2026-08-21 (3 weeks)
**Status:** 🟢 On plan. Day 1 code work complete; Phase 0 runs Monday.

---

## Time log

| Task | Hrs |
|---|---|
| Architecture review + 3-week phase plan (`KICKOFF.md`) — day-by-day sequencing, `[GHL]` vs `[CODE]` split, owners, risk register | 2.0 |
| Repo scaffold, canonical doc consolidation, `CLAUDE.md` build rules | 1.0 |
| Contracts package — shared key (§5), enums (§7/§6.3/§6.4/§6.7), client gate (§9.1), auth tiers (§9.2), internal-field deny-list (§9.3), handoff payload (§8.2) + tests | 2.5 |
| `Project` schema mirror (§6.1, 49 fields) + publishing state machine (§10) + tests | 1.5 |
| Phase 0 go/no-go runbook (`docs/PHASE-0.md`) | 1.0 |
| **Total** | **8.0** |

---

## Delivered

Repo at `ghl-portal/`, with the architecture canonical and everything sequenced
around it. **34 tests green, typecheck clean.**

- **`KICKOFF.md`** — the 3-week plan. Every line tagged `[GHL]` (config work in
  the GHL UI, humans only) or `[CODE]` (this repo), so the split is visible
  rather than assumed.
- **`docs/PHASE-0.md`** — Monday's go/no-go, step by step, with fixture values,
  a results table, and the fallback each failure triggers.
- **`packages/contracts`** — the architecture's invariants as running code.
- **`CLAUDE.md`** — architecture wins, don't invent schema, the four invariants
  most likely to break.

Two tests are doing real work rather than checking their own homework:

- **No cross-project leakage** — Client A's request cannot resolve Client B's
  records, enforced at the data layer rather than in a screen.
- **No path from field to client without a PM** — a breadth-first search over
  every legal move a field user can make proves they only ever reach `DRAFT`
  and `PENDING`. §3.2 is enforced now, not just documented.

The §6.1 schema mirror and §10 state machine were Day 2–3 items, pulled forward.
`demoableCoreFields()` returns the exact ten-field build list for the `Project`
object, so Phase 1 starts from a checklist.

---

## Monday (Day 1) — Chris + Pat

**Run `docs/PHASE-0.md`. ~1h15. Nothing in Phase 1+ starts until it passes.**

Prerequisite checks first — Custom Objects, native Client Portal, snapshot
behaviour, **on the contractor sub-account tier, not the agency view**. Then
Test A (~10 min) and Test B (~1 hr).

One ask on Test B: note **how** the value bound (native binding, API call,
manual) and whether it can **filter by the signed-in contact**. That answers F1
for free — and if it can't filter, Phase 4 needs rethinking before anyone builds
a screen.

**Sing:** the Send-to-CRM extension is Week 1's longest external lead time.
Starting Monday rather than Wednesday keeps Friday's exit criteria intact.

---

## Blockers

| # | Question | Needed by |
|---|---|---|
| **F1** | Dashboard/portal *entirely* AI Studio with me writing only the data layer, or AI Studio as prototype and we build a real front-end on the GHL API? ~3 days vs ~8 in Week 2. | Day 2 |
| **F2** | WF1–WF8 GHL-native or n8n? GHL-native survives the Phase 6 snapshot; n8n becomes a per-account setup step. | Day 3 |
| **F3** | GHL integration token + API base URL for the build sub-account. | Day 3 |
| **F4** | Where the sync-back job runs and what it writes into — endpoint Sing exposes, Supabase table, or direct write? | Day 3 |

Sync-back job and all UI work are on hold until F1 and F2 land. Test B will
likely answer F1 on its own.

---

## Flags

- **Stage count: 19 or 20?** ARCHITECTURE §7 lists 19 sequential; the kickoff PDF
  says 20. Architecture wins per §0, so the code encodes 19 — but whoever builds
  the pipeline should know before they start counting.
- **Financial gate: AND or OR?** §9.3 writes it as "Show Budget to Client / Show
  Detailed Pricing" without saying which. Implemented as OR, flagged in code.
- **`Show Schedule to Client` is named but never mapped to any field.** Probably
  the Dates group — a guess, so not encoded.
- **Phase 6 spills.** Building the snapshot fits inside Aug 21; validating it in
  Alliance Pro Services, a live sub-account, does not. Plan Aug 24–25, or ship
  Phase 6 validated in the *test* sub-account and Alliance goes week 4.
- **3 weeks vs the docs' own 3–5 week V1 estimate.** Weeks 1–2 (the demoable
  slice) are committed scope; Week 3's V1 remainder is the compressible surface.
