# EOD — BuildSuite™ Three-Experience Platform

**Date:** 2026-08-04 (Tue) · **Total: 12.0 hrs**
**Tests:** 88 green · **Build:** clean · **Pushed:** `github.com/Dale260839/ghl-portal`

---

## Time log

| Task | Hrs |
|---|---|
| Decisions D-001 → D-004 recorded; Phase 0 Test B rewritten; live-system guardrails added to `CLAUDE.md` | 1.5 |
| Next.js app scaffold, workspace wiring, design system | 1.0 |
| Auth + session, role-gated routing, sign-in for the three experiences | 1.0 |
| Contractor Dashboard — Portfolio Overview, Projects List, Project Overview, Field Update review queue | 2.0 |
| Client Portal — gated data layer, project switcher, schedule, budget, published updates | 1.5 |
| Field Interface — mobile today view, tasks, daily update form | 0.5 |
| Repo + git setup, GitHub push, docs | 0.5 |
| Re-phased remaining plan around credentials; `.env.example` covering every key | 0.5 |
| GHL API client, error taxonomy, retry/backoff, response mapper, `GhlDataSource` | 1.0 |
| Gate + leak checks converted from manual verification into an automated suite | 0.5 |
| WF1 + WF2 as pure tested planners — 21 tests asserted against §11 action by action | 1.5 |
| Implementation KB written and moved into the repo | 0.25 |
| Integration ask for Sing — every blocked item with the exact contracts | 0.25 |
| **Total** | **12.0** |

---

## What shipped

**Four decisions closed** — D-001 front-end built in this repo not AI Studio ·
D-002 workflows are application code · D-003 direct GHL API access with
guardrails · D-004 sync-back through Sing's BuildSuite API.

**A working app**, all three experiences, running end to end on fixtures.
Contractor Dashboard (four screens, incl. the review queue with its seven
verbatim actions), Field Interface (mobile, tasks, daily update form), Client
Portal (project switcher, progress, schedule, budget, published updates).

**The GHL client** — typed errors, jittered retry/backoff, response mapper,
`GhlDataSource` behind the interface the screens already use.

**WF1 + WF2** as pure planners: trigger in, typed effects out. No network, no
credentials. Both idempotent; both refuse to act on a missing shared key rather
than falling back to name matching.

**The leak checks are now tests** — 14 of them, including value-level checks that
survive a field rename. 88 tests total (54 web, 34 contracts).

**Two documents** — the implementation KB, and `What_We_Need_From_Sing.md`
covering every blocked item with the exact contracts we validate against.

---

## Not real yet

Fixtures (bannered on every screen) · demo cookie, not GHL login · publish state
in memory · mapper and endpoint paths provisional · WF1/WF2 plan effects but
nothing executes them yet.

**Phase 0 still hasn't reported back.** It gates the next phase harder than the
keys do.

---

> **Hours:** 12.0 is the honest figure. If the convention is a flat 8.0, the last
> three rows carry to tomorrow.
> **Scope:** GHL platform only — today's Material Takeoff work is a separate
> project and isn't counted here.
