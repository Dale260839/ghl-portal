# EOD — BuildSuite™ Three-Experience Platform

**Date:** 2026-08-12 (Wed) · **Total: 8.0 hrs**
**Tests:** 132 green (98 web, 34 contracts) · **Build:** clean · **Pushed:** `github.com/Dale260839/ghl-portal`

Three things: **the Hub is reading live production data**, **Phase 3's six screens
are complete**, and **six decisions landed** — four of which delete work rather
than add it.

---

## Time log

| Task | Hrs |
|---|---|
| Reviewed Sing's BuildSuite API reference; recorded D-005 and traced its consequences | 0.75 |
| D-006 / D-008 — auth model, distinct-system boundary, and the cookie-domain conflict between them | 0.75 |
| D-007 / D-009 — sync-back reassigned, data access moved to Supabase-direct; rewrote Sing's ask, updated `.env.example` | 1.0 |
| Credentials wired into `.env.local`; live read-only verification and schema discovery against production | 0.75 |
| D-010 — confirmed the shared key doesn't exist; join-key recommendation and the PII finding | 0.75 |
| BuildSuite read-only client + projects reader + 15 tests | 1.75 |
| `/dashboard/buildsuite` live screen, nav, verified against production | 0.75 |
| Client Visibility Settings screen + 8 tests — **Phase 3 complete** | 1.5 |
| **Total** | **8.0** |

---

## Live data, for the first time

`/dashboard/buildsuite` reads BuildSuite's Supabase directly and renders real
projects — 43 active, with counts across `matched` / `new` / `draft` /
`completed`. Badged **"Live data"**: the inverse of the fixture banner, because
the notable thing about this screen is that it isn't a demo. A read failure shows
an error rather than quietly falling back to fixtures.

**Read-only by construction, not by promise.** D-003 says never alter that
database, and D-010 showed the key permits more than it should, so:

- The client exposes exactly two methods — `select` and `count`, both GET. There
  is no `insert`/`update`/`delete`/`rpc` to reach for by accident, and a test
  asserts that surface stays that way.
- **`select(*)` is refused**; columns must be named. That stops a schema addition
  on Sing's side silently widening what we pull, and keeps us out of columns we
  have no screen for — `client_email`, `client_phone`, `scope`, `sow_data`,
  `documents` are deliberately not selected even though the key would return them.
- `count()` uses PostgREST's `Content-Range`, so status tallies transfer no rows.

Column names came from the live schema, not guesses.

---

## Phase 3 complete — Client Visibility Settings

The sixth and last §12.1 screen. Switches on the left are clauses of the §9.1
gate, so it's contractor-only and checked server-side rather than by hiding the
form.

**The panel on the right isn't a mock-up of their effect.** It runs the actual
gate and the actual allow-list projection against that project and its real
contact, then reports what came back. Toggle a switch, save, and the panel changes
because the projection changed — not because a second copy of the rules agreed
with the first. Turn the master switch off and it says the client sees nothing,
quoting the gate's denial reason. It also lists the §9.3 deny-list explicitly, so
the boundary is visible rather than merely trusted.

Two details worth recording:

- **`setVisibility` takes the full switch set, never a patch.** An HTML form omits
  unchecked boxes, so a patch-shaped API can't tell "unchecked" from "not
  submitted" — and the safe-looking reading of that ambiguity is exactly wrong
  when someone is trying to turn something off.
- **The load-bearing test is a drift check.** A switch on `Project` but missing
  from `VISIBILITY_SWITCHES` would be silently un-editable: not rendered, not
  submitted, read as `false`, turned off. A contractor would believe they
  controlled something they didn't.

Also fixed a missing `.ts` extension on `mutations.ts` that had been keeping the
whole module out of `node --test`.

---

## Six decisions — four of them delete work

| # | Decision | Effect |
|---|---|---|
| **D-005** | BuildSuite's API has **no machine authentication** — every endpoint needs a `bs_session` cookie from the GHL login flow | `BUILDSUITE_API_KEY` never existed. A scheduled server-to-server job cannot call it. |
| **D-006** | The Hub authenticates the way BuildSuite does — GHL login | No second identity system |
| **D-007** | BuildSuite already holds a **GHL private integration key** | The stage sync-back likely belongs on Sing's side, not ours — proposed to him |
| **D-008** | The Hub is a **distinct system** with its own two keys | Routes around D-005 entirely |
| **D-009** | Data access is **Supabase-direct**; BuildSuite's API is out of scope | Closes D-004, moots D-005. No service token, no broken endpoint, no id-space trap. |
| **D-010** | **`BSP-YYYY-NNNNNN` does not exist** in BuildSuite's database | The join key is already GHL-native |

### D-010 needs a decision from Chris

`projects` has **53 columns and none of them is a BuildSuite Project ID.** No
`bsp_*`, no `BSP-` value in any sampled row. What every row *does* carry:
`ghl_contact_id` and `ghl_opportunity_id`.

BuildSuite already links to GHL, using GHL's own identifiers. The shared key we
specified doesn't need inventing — a working one is in place on both sides.

**Recommendation: adopt `ghl_opportunity_id` and amend ARCHITECTURE §5/§3.6**
rather than ask Sing for a production schema change plus backfill to introduce a
duplicate key. §5's real requirement is *one immutable shared key, never match on
name or address* — `ghl_opportunity_id` satisfies it. The `BSP-` format was a
preference, not the requirement.

Cost if approved: roughly a morning. `ids.ts` and the handoff contract currently
**reject** anything that isn't `BSP-YYYY-NNNNNN`.

### A conflict worth catching now

D-006 has the Hub using `bs_session`, scoped to `.buildsuite.ai`. ARCHITECTURE §2
puts the contractor surfaces at `projects.<contractordomain>`. **A cookie scoped
to one domain is never sent to the other** — a browser rule, not a config option.
So either the Hub lives under BuildSuite's domain, or it owns its own GHL-backed
session. **Recommending the latter:** the first is faster but permanently couples
the Hub's URL to BuildSuite's domain, and un-picking it later means
re-authenticating every user on every sub-account.

---

## For Sing — one security item

The **publishable** key reads `contractors` including `full_name` and `phone`; we
saw a real contractor's mobile number on the first row. Publishable keys are
designed to be embedded in browser bundles, so in practice that table is readable
by anyone who has ever loaded BuildSuite's front-end.

Not our table and we won't touch it — but far cheaper to fix before someone
notices. Either RLS should scope it or the Hub gets a differently-scoped key.
Written up as information, not a request.

It also corrected an assumption of mine: I'd expected the read key to be
RLS-restricted. It isn't, for those tables. We read only `projects` and treat the
rest as off-limits regardless of what the key permits.

---

## Still blocked

**`GHL_LOCATION_ID` and `GHL_PROJECT_OBJECT_KEY`.** The private integration key
works, but alone it can't address a sub-account or a custom object. Those two
strings are all that stand between us and the contractor dashboard running fully
live.

**Phase 0 has still not reported back.**

---

## Next — Phase 4, Client Portal connected

Its screens are already built. The two pieces left are real authentication
(blocked on the hosting decision) and live project data (blocked on the two
strings above). **We're ahead on surface area and behind on connection**, and
connection is where the blockers sit.

Seven working days to Aug 21. If those two strings slip past mid next week, the
honest call is demoing the chain with the BuildSuite half live and the GHL half on
fixtures.

Buildable meanwhile, in order: **WF7 (Issue Submitted)** — V1 scope, fully
specified, and right now WF3 creates issues that nothing displays; then the
**Issues screen**; then refreshing `docs/kb/session-context.md`, which predates
D-007 through D-010 and is the file most likely to mislead a fresh session.

---

> **Scope:** GHL platform only. Material Takeoff work is a separate project and
> isn't counted here.
